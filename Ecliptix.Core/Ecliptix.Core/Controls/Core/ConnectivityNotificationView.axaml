<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:Ecliptix.Core.Controls.Core"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d" d:DesignWidth="350" d:DesignHeight="60"
             x:Class="Ecliptix.Core.Controls.Core.ConnectivityNotificationView"
             x:Name="Root"
             x:DataType="core:ConnectivityNotificationViewModel">

    <UserControl.Resources>
        <system:TimeSpan x:Key="RestoredStateDuration">0:0:3</system:TimeSpan>
    </UserControl.Resources>

    <UserControl.Styles>
        <Style Selector="Ellipse.Pulse">
            <Style.Animations>
                <Animation Duration="0:0:1.5" IterationCount="INFINITE">
                    <KeyFrame Cue="0%"><Setter Property="Opacity" Value="0.4"/></KeyFrame>
                    <KeyFrame Cue="50%"><Setter Property="Opacity" Value="1.0"/></KeyFrame>
                    <KeyFrame Cue="100%"><Setter Property="Opacity" Value="0.4"/></KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>

        <Style Selector="Border.Offline">
            <Setter Property="Background" Value="#E5E7EB"/>
        </Style>

        <Style Selector="Border.ServerIssue">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                        <GradientStop Color="#fef2f2" Offset="0"/>
                        <GradientStop Color="#fff1f2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#99fecaca"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style Selector="Border.Restored">
            <Setter Property="Background" Value="#BBF7D0"/>
        </Style>


        <Style Selector="Border#PART_TimerBar">
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="RenderTransform" Value="scaleX(1)"/>
            <Setter Property="Background" Value="#22C55E"/>
        </Style>

        <Style Selector="Border.Restored Border#PART_TimerBar">
            <Setter Property="Opacity" Value="1"/>
            <Style.Animations>
                <Animation Duration="{StaticResource RestoredStateDuration}"
                           FillMode="Forward"
                           Easing="LinearEasing">
                    <KeyFrame Cue="0%">
                        <Setter Property="ScaleTransform.ScaleX" Value="1"/>
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="ScaleTransform.ScaleX" Value="0"/>
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>

    <Border x:Name="MainBorder"
            CornerRadius="12"
            BoxShadow="0 4 24 4 #15000000"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">

        <Border x:Name="ContentBorder"
                Background="White"
                Margin="4,0,0,0"
                CornerRadius="8,12,12,8"
                ClipToBounds="True">

            <Grid>
                <Grid Margin="16,12" ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Image x:Name="StatusIcon"
                           Width="24" Height="24"
                           Grid.Column="0"
                           VerticalAlignment="Center"
                           Source="{Binding StatusIconSource}"
                           Stretch="Uniform">
                        <Image.Styles>
                            <Style Selector="Image">
                                <Setter Property="Opacity" Value="1"/>
                            </Style>
                            <Style Selector="Border.ServerIssue Image">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Style>
                        </Image.Styles>
                    </Image>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                        <TextBlock x:Name="StatusTextBlock"
                                   Text="{Binding StatusText}"
                                   FontFamily="{StaticResource SegoeUI}"
                                   Foreground="#111827"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   TextWrapping="NoWrap">
                            <TextBlock.Styles>
                                <Style Selector="TextBlock">
                                    <Setter Property="Foreground" Value="#111827"/>
                                </Style>
                                <Style Selector="Border.ServerIssue TextBlock">
                                    <Setter Property="Foreground" Value="#b91c1c"/>
                                </Style>
                            </TextBlock.Styles>
                        </TextBlock>

                        <TextBlock x:Name="StatusDescriptionBlock"
                                   Text="{Binding StatusDescription}"
                                   FontFamily="{StaticResource SegoeUI}"
                                   Foreground="#6B7280"
                                   FontSize="12"
                                   TextWrapping="NoWrap">
                            <TextBlock.Styles>
                                <Style Selector="TextBlock">
                                    <Setter Property="Foreground" Value="#6B7280"/>
                                </Style>
                                <Style Selector="Border.ServerIssue TextBlock">
                                    <Setter Property="Foreground" Value="#dc2626"/>
                                </Style>
                            </TextBlock.Styles>
                        </TextBlock>
                    </StackPanel>

                    <Button x:Name="RetryButton"
                            Grid.Column="2"
                            Margin="8,0,0,0"
                            Padding="12,6"
                            CornerRadius="8"
                            Background="Transparent"
                            BorderBrush="#E5E7EB"
                            BorderThickness="1"
                            Command="{Binding RetryCommand}"
                            IsVisible="{Binding ShowRetryButton}">

                        <Button.Styles>
                            <Style Selector="Button:pointerover /template/ ContentPresenter">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                            </Style>
                        </Button.Styles>

                        <TextBlock Text="{Binding RetryButtonText}"
                                   FontFamily="{StaticResource SegoeUI}"
                                   Foreground="#374151"
                                   FontWeight="Medium"
                                   FontSize="12"/>
                    </Button>

                    <Ellipse x:Name="StatusDot"
                             Grid.Column="3"
                             Width="8" Height="8"
                             Margin="8,0,0,0"
                             VerticalAlignment="Center"
                             Classes="Pulse"
                             Fill="#EF4444"
                             IsVisible="{Binding IssueCategory, Converter={x:Static ObjectConverters.IsNotNull}}"/>
                </Grid>

                <Border x:Name="PART_TimerBar"
                        VerticalAlignment="Bottom"
                        HorizontalAlignment="Left"
                        Height="3"
                        Width="{Binding Bounds.Width, ElementName=ContentBorder}"
                        RenderTransformOrigin="0%,0%">
                        </Border>
            </Grid>
        </Border>
    </Border>
</UserControl>
