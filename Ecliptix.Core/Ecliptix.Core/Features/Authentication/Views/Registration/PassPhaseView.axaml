<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Ecliptix.Core.Controls"
             xmlns:coreControls="clr-namespace:Ecliptix.Core.Controls.Core"
             xmlns:registration="clr-namespace:Ecliptix.Core.Features.Authentication.ViewModels.Registration"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d"
             d:DesignWidth="{StaticResource Content.MaxWidth}"
             d:DesignHeight="200"
             x:Class="Ecliptix.Core.Features.Authentication.Views.Registration.PassPhaseView"
             x:DataType="registration:PassPhaseViewModel"
             MinWidth="{StaticResource Content.MaxWidth}"
             Width="{StaticResource Content.MaxWidth}">

    <!-- All resources moved to shared Core styles -->

    <!-- All styles moved to shared Components/Buttons.axaml -->

    <Grid VerticalAlignment="Top"
          RowSpacing="{StaticResource Spacing.XL}"
          RowDefinitions="Auto,Auto,Auto"
          HorizontalAlignment="Stretch"
          Width="{StaticResource Content.MaxWidth}">

        <!-- Header Section -->
        <StackPanel Grid.Row="0"
                    Spacing="{StaticResource Spacing.S}"
                    MaxWidth="{StaticResource Header.MaxWidth}"
                    HorizontalAlignment="Left">
            <TextBlock Text="{Binding LocalizationService[Authentication.SignUp.PassPhase.Title]}"
                       FontSize="{StaticResource Font.XXL}"
                       FontWeight="Light"
                       Foreground="{DynamicResource OnSurface}"
                       LineHeight="28" />
            <TextBlock Text="{Binding LocalizationService[Authentication.SignUp.PassPhase.Description]}"
                       TextWrapping="Wrap"
                       FontSize="{StaticResource Font.Title.M}"
                       LineHeight="28"
                       FontWeight="Normal"
                       Foreground="{DynamicResource OnSurface}"
                       Opacity="0.8" />
        </StackPanel>

        <!-- Segmented Pass Phase Input -->
        <coreControls:SegmentedTextBox Grid.Row="1"
                                   SegmentCount="4"
                                   Height="60"
                                   FontSize="{StaticResource Input.SegmentFontSize}"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   SegmentBackground="{DynamicResource Surface}"
                                   SegmentSpacing="5"
                                   SegmentWidth="40"
                                   AllowOnlyNumbers="True"
                                   Value="{Binding PassPhase, Mode=TwoWay}"
                                   ActiveSegmentBorderColor="{DynamicResource Primary}"/>

        <!-- Submit Button -->
        <Button Grid.Row="2"
                Content="{Binding LocalizationService[Authentication.SignUp.PassPhase.Button]}"
                Command="{Binding SubmitCommand}"
                IsEnabled="{Binding SubmitCommand.CanExecute}"
                Classes="auth-button continue-button"
                Margin="0,10,0,0" />
    </Grid>
</UserControl>