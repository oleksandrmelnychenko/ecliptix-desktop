<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Ecliptix.Core.Controls"
             xmlns:registration="clr-namespace:Ecliptix.Core.ViewModels.Authentication.Registration"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="registration:PhoneVerificationViewModel"
             x:Class="Ecliptix.Core.Views.Authentication.Registration.PhoneVerificationView">
   
    <Grid VerticalAlignment="Center" RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="10" MinWidth="350">
        
        <Grid RowDefinitions="Auto, Auto">
            <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="24" FontWeight="Light" Foreground="#333333" />
            <TextBlock Grid.Row="1" Text="{Binding Description}" FontSize="14" Margin="0,4,0,10" Foreground="#B0B0B0" />
        </Grid>
        

        <controls:HintedTextBox
            Grid.Row="1"
            x:Name="MobileTextBox"
            Watermark="{Binding Hint}"
            Hint="{Binding Hint}"
            Text="{Binding Mobile, Mode=TwoWay}"
            Margin="0,5"
            CornerRadius="15"
            BorderBrush="#E0E0E0"
            TextForeground="#333333"
            HintForeground="#6a5acd"
            IconSource="/Assets/lamp.svg"
            ValidationType="PhoneNumber" />

        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" />

        <Button
            Grid.Row="3"
            Classes="SubmitButton"
            Content="{Binding ButtonContent}"
            Command="{Binding VerifyMobileCommand}" />
    </Grid>
  
</UserControl>